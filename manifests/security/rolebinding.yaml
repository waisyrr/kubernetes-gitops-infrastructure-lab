apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: nginx-reader-binding
  namespace: demo

subjects:
  - kind: ServiceAccount
    name: nginx-sa
    namespace: demo

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: nginx-reader